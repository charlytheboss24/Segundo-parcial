palabra = input("Ingresa una palabra: ")
letra = input("Ingresa una letra: ")

contador = 0

for l in palabra:
    if l == letra:
        contador += 1

print("La letra aparece", contador, "veces en la palabra.")
